name: Build Kernel A71 Android 16 Docker

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Kernel
        uses: dabao1955/kernel_build_action@main
        with:
          kernel-url: https://github.com/LineageOS/android_kernel_samsung_a71
          kernel-branch: lineage-23.2
          config: a71_defconfig
          arch: arm64
          
          # TẮT LXC TỰ ĐỘNG ĐỂ TRÁNH LỖI CẤU TRÚC MÃ NGUỒN (cstate error)
          lxc: false
          lxc-patch: false
          
          # SỬ DỤNG MIRROR CRDROID NHÁNH 14
          aosp-clang: false
          other-clang-url: https://gitlab.com/crdroidandroid/android_prebuilts_clang_host_linux-x86_clang-r530567.git
          other-clang-branch: 14 # Đã sửa từ 15 thành 14
          
          # SETUP GCC MỒI (Android 12)
          aosp-gcc: true
          android-version: 12
          
          anykernel3: true
          release: true
          access-token: ${{ secrets.GITHUB_TOKEN }}
          
          # THAM SỐ ÉP BIÊN DỊCH
          extra-make-args: '["LLVM=1", "LLVM_IAS=1", "KCFLAGS=-w", "CC=clang", "LD=ld.lld"]'
